# 🚀 빠른 시작 가이드

## 1단계: 환경 설정

### Private Key 설정 (권장)

터미널에서 다음 명령어를 실행하세요:

```bash
export PLAYER_PRIVATE_KEY=0x5a8958f00e3a4ae763ab2e44cb634268bc6bb5e27283ccb93a1002e874d05c93
```

> 💡 **참고:** 환경변수를 설정하지 않으면 프로그램 시작 후 수동으로 입력해야 합니다.

---

## 2단계: CLI 실행

### 방법 1: 스크립트 사용 (가장 간단)

```bash
./run-cli.sh
```

### 방법 2: Gradle 직접 사용

```bash
./gradlew bootRun --args='--app.mode=cli --WEB_APP_TYPE=none'
```

---

## 3단계: 게임 시작

### 메뉴 설명

```
1. 게임 참가 (Token Approve)    ← 먼저 실행!
2. 숫자 제출 (Submit Number)    ← 1, 2, 3 중 선택
3. 새 라운드 시작
4. 현재 라운드 정보 조회
5. 플레이어 목록 조회
6. 승자 조회
7. 컨트랙트 잔액 조회
8. 내 토큰 잔액 조회
9. Allowance 조회
0. 종료
```

### 게임 플레이 순서

1. **메뉴 1번** - Token Approve (최초 1회)
   ```
   선택> 1
   Approve할 토큰 양 (ETH 단위) > 100
   ```

2. **메뉴 2번** - 숫자 제출
   ```
   선택> 2
   제출할 숫자 (1, 2, 또는 3) > 2
   베팅 금액 (ETH 단위) > 10
   ```

3. **메뉴 4번** - 라운드 정보 확인
   ```
   선택> 4
   ```

4. **메뉴 6번** - 승자 확인
   ```
   선택> 6
   ```

---

## 문제 해결

### Q: "입력을 받을 수 없습니다" 에러

**원인:** 환경변수가 설정되지 않았고, 표준 입력이 연결되지 않음

**해결:**
```bash
# 환경변수 설정
export PLAYER_PRIVATE_KEY=0x...

# 다시 실행
./run-cli.sh
```

---

### Q: "Approve 실패" 에러

**원인:** 토큰 잔액 부족 또는 네트워크 문제

**해결:**
1. 메뉴 8번으로 토큰 잔액 확인
2. Sepolia Testnet에 연결되어 있는지 확인
3. Faucet에서 테스트 토큰 받기

---

### Q: REST API도 사용하고 싶어요

**해결:**
```bash
# 터미널 1: REST API 실행
./run-web.sh

# 터미널 2: CLI 실행
./run-cli.sh
```

별도 터미널에서 각각 실행하면 동시 사용 가능합니다!

---

## 유용한 명령어

### 환경변수 영구 설정 (선택사항)

**Bash 사용자:**
```bash
echo 'export PLAYER_PRIVATE_KEY=0x...' >> ~/.bashrc
source ~/.bashrc
```

**Zsh 사용자:**
```bash
echo 'export PLAYER_PRIVATE_KEY=0x...' >> ~/.zshrc
source ~/.zshrc
```

### 빌드 및 실행

```bash
# 빌드
./gradlew clean build -x test

# JAR로 실행
java -jar build/libs/java_eth_demo-0.0.1-SNAPSHOT.jar \
  --app.mode=cli \
  --WEB_APP_TYPE=none
```

---

## 추가 도움말

- 상세 가이드: `CLI_GUIDE.md` 참고
- 문제 발생 시: 에러 메시지와 함께 문의
- 테스트 네트워크: Sepolia 권장

---

**즐거운 게임 되세요! 🎮✨**

